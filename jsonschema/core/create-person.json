{
  "title": "core.create_person(json)",
  "description": "expected JSON payload for core.create_person(json)",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string",
      "format": "email"
    }
  },
  "required": ["name", "email"]
}
