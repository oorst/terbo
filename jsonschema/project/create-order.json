{
  "title": "prj.create_order(json)",
  "description": "expected JSON payload for prj.create_order(json)",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "integer"
    }
  },
  "required": ["projectId"],
  "additionalProperties": false
}
