{
  "title": "sales.update_invoice(json)",
  "description": "expected payload for sales.update_invoice(json)",
  "properties": {
    "invoiceId": {
      "type": "integer"
    },
    "dueDate": {
      "type": "string"
    },
    "issueDate": {
      "type": "string"
    },
    "period": {
      "type": "integer"
    },
    "status": {
      "type": "string",
      "enum": ["DRAFT", "ISSUED", "DELETED", "VOID"]
    },
    "notes": {
      "type": "string"
    }
  },
  "required": ["invoiceId"],
  "additionalProperties": false
}
