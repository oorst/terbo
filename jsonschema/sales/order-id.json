{
  "title": "order_id",
  "description": "expected JSON payload for functions that require an order_id",
  "type": "object",
  "properties": {
    "order_id": {
      "type": "integer"
    }
  },
  "required": ["order_id"]
}
