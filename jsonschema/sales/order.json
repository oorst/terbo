{
  "title": "sales.order",
  "description": "expected JSON payload for sales.order(json)",
  "type": "object",
  "properties": {
    "order_uuid": {
      "type": ["string"]
    }
  },
  "required": ["order_uuid"]
}
