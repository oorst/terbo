{
  "title": "sales.create_order(json)",
  "description": "expected payload for sales.create_order(json)",
  "properties": {
    "customer_uuid": {
      "type": "string",
      "description": "party_uuid of the buyer for the order"
    }
  },
  "required": ["customer_uuid"]
}
