{
  "title": "invoice_id",
  "description": "expected JSON payload for functions that require an invoice_id",
  "type": "object",
  "properties": {
    "invoice_id": {
      "type": "integer"
    }
  },
  "required": ["invoice_id"]
}
