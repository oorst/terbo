{
  "title": "product_id",
  "description": "expected JSON payload for functions that require a product_id",
  "type": "object",
  "properties": {
    "product_uuid": {
      "type": "string"
    }
  },
  "required": ["product_uuid"]
}
